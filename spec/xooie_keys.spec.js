require(['jquery', 'xooie/xooie_keys'], function($, XooieKeys){
	describe('XooieKeys', function(){

		describe('When instantiating the module...', function() {

			it('returns the same instance each time it is instantiated', function() {

			});

			it('binds a keyup event to the document', function() {

			});

		});

		describe('Default keybindings', function() {
			it('moves to the next focusable item', function() {

			});
		});

		describe('Set', function() {
			it('sets the function to be called when the key with the specified selector is pressed', function() {

			});

			it('sets all the keys to the corresponding functions when passed in as key-value pairs', function() {

			});

			it('casts a string number as an integer', function() {

			});
		});

		describe('Call', function() {
			it('calls the function associated with the key', function() {

			});
		});

	});
});